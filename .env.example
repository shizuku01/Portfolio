# =============================================================================
# ENVIRONMENT VARIABLES - EXAMPLE / TEMPLATE
# =============================================================================
# Copy this file to ".env" (which is gitignored) and fill in real values.
# On the server:   cp .env.example .env   then edit .env
#
# Docker Compose automatically loads ".env" from the project directory.

# --- Contact form email (Gmail) ---------------------------------------------
# 16-character Gmail App Password used by the contact form to send email.
# Generate one at: https://myaccount.google.com/apppasswords
# Leave blank if you are not using the contact form yet.
EMAIL_PASSWORD=

# Gmail account that sends the mail (must match the App Password above).
# Optional — defaults to the address baked into server.js.
EMAIL_USER=

# Where contact-form messages are delivered. Optional — defaults to EMAIL_USER.
EMAIL_TO=
